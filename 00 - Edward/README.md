#Edward\n
it's an simple Toy OS that can read n creat files, it also has password
#Password
U can define a password with defPassword and it'll be saved as a simple hash for comparison later
u'll use it if u wanna write some protected file 'cause if u wanna read it u must use the password
u also can use the comand Password to test the Password and the hashPassword to see how it was saved
#files
using the cmd writeAt u can create or rewrite a file, if u're creating it, u can decide if u wanna it protected or not
u can read the file using Read n if it's protected, u'll also need to write ur password
